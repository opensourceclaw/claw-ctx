# Task: claw-ctx v6.5.0 — MECW-Aware Compaction 实现

**From**: Friday (A)
**To**: Jarvis (B)
**Date**: 2026-08-13
**Stage**: CODE → BUILD → TEST
**Priority**: P1
**PipelineId**: pipeline-20260813-ctxv650
**Project**: claw-ctx
**Version**: v6.5.0

---

## ⚠️ Inbox 绝对路径

```
Read:   /Users/liantian/workspace/osprojects/claw-ctx/inbox/inbox-code/
Write:  /Users/liantian/workspace/osprojects/claw-ctx/inbox/inbox-results/
Design: /Users/liantian/workspace/osprojects/claw-ctx/inbox/inbox-plan/v6.5.0-mecw-compaction-design.md
Repo:   /Users/liantian/workspace/osprojects/claw-ctx/
```

## 背景与目标

压缩阈值目前是静态的，未适配 MECW。详细设计见 Design 路径。目标：`MECW = maxContextTokens × effectiveWindowRatio × complexityFactor(taskType)`，并接入 ProactiveCompactionController。

## 实现任务（按设计）

### 1. 新增 `src/mecw/MecwEstimator.ts`
- `MecwEstimate` 接口（modelId / taskType / maxContextTokens / effectiveWindowRatio / complexityFactor / mecwTokens）
- `MecwEstimator` 类：`estimateMecw(modelId, taskType)` / `getComplexityFactor(taskType)` / `getFactors()`
- 默认 `DEFAULT_COMPLEXITY_FACTORS`：SIMPLE_LOOKUP=1.0, MULTI_LOOKUP=0.8, SUMMARIZATION=0.7, COMPLEX_REASONING=0.6
- 复用 `ModelAwareOptimizer.getOptimizationHint(modelId)` 取 maxContextTokens / effectiveWindowRatio

### 2. 打通 ProactiveCompactionController
- `shouldCompact` 新增可选 `taskType?: ContextTaskType` 参数
- 传 taskType 时 threshold = MECW；未传时回退现有静态 compressionThreshold（向后兼容）
- cooldown / limit / minTokens 逻辑不变

### 3. claw-mem 集成（压缩前 flush）
- compact 触发前 flush 内存（保留重要记忆），claw-mem 不可用时优雅降级

### 4. 导出
- `src/index.ts` 导出 MecwEstimator / MecwEstimate / DEFAULT_COMPLEXITY_FACTORS
- `src/mecw/index.ts`

## 测试需求（+12 文件，目标 94+ files）
见设计文档测试需求表（mecw/ 下 7 个核心 + 5 个补充）。

## 验收标准
- [ ] MecwEstimator 公式正确，factor 表正确且可配置
- [ ] shouldCompact 接 MECW，未传 taskType 向后兼容
- [ ] claw-mem flush 集成 + 优雅降级
- [ ] 复杂任务 MECW < 简单任务
- [ ] 94+ files 全绿，build 零错误
- [ ] 不破坏 v6.4.0 的 3 工具 + compaction 修复（零回归）
- [ ] 不要 git commit（Friday 提交）

## 输出文档
完成后在 `inbox/inbox-results/pipeline-20260813-ctxv650-complete.md` 创建报告（附真实 build/test 数字）。

## ⚠️ 提醒
- ✅ 三权分立：不 commit；❌ 不 push/release
- ⚠️ 如实报告，勿夸大（前几轮有数字不实教训）

---

**启动时间**: 2026-08-13 22:56 GMT+8
