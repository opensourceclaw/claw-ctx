# Report: claw-ctx v5.11.1 - Token 自动压缩失效修复

**Status**: completed
**From**: Jarvis (B)
**To**: Friday (A)
**Date**: 2026-07-22
**Stage**: BUILD
**Project**: claw-ctx
**Version**: v5.11.1

---

## TL;DR

修复 OpenClaw `2026.7.x` + claw-ctx 组合下 token 自动压缩失效问题。**根因不是 v5.11.0 引入的 bug**，而是 OpenClaw 升级后契约不匹配：claw-ctx 声明 `ownsCompaction: true`，OpenClaw 据此跳过 `[context-overflow-precheck]` 但从不读取 `assembled.autoCompact` 标志，导致无人触发 `compact()`。

修复方式：在 `afterTurn()` 末尾加自触发逻辑，超 85% 阈值主动调用 `compact()`。

---

## 1. 现象

- 最后一次成功压缩日志：`2026-07-18T01:00:35.908 [plugins] [claw-ctx] COMPACT: 2042 msgs -> 1750 msgs, 180987 -> ≈143519 tokens`
- 2026-07-18 12:29 OpenClaw 升级到 `2026.7.1-2`
- 之后 3 天无 COMPACT 日志，会话文件膨胀到 506KB / 85 条消息仍未触发压缩

## 2. 根因分析

### 调用契约不匹配

**OpenClaw `2026.7.1-2` 行为** (`selection-JInn13lc.js:14134-14142`)：

```js
const shouldSkipPrecheck = skipPromptSubmission ||
  contextEngineAssemblySucceeded &&
  activeContextEngine?.info.ownsCompaction &&
  contextEnginePromptAuthority !== "preassembly_may_overflow";
// -> "[context-overflow-precheck] skipped: context engine claw-ctx owns compaction"
```

**claw-ctx 声明** (`src/engine.ts:204`)：

```typescript
const INFO = { id: "claw-ctx", ..., ownsCompaction: true, turnMaintenanceMode: "foreground" };
```

结果：OpenClaw 把"何时压缩"的责任完全交给 claw-ctx，但：

1. **OpenClaw 不读 `assembled.autoCompact` 标志** (`selection-JInn13lc.js:7505-7511` 只消费 `messages/systemPromptAddition/promptAuthority`)
2. **claw-ctx `afterTurn()` 不主动调用 `compact()`**（原实现只做 ingest / self-refine / checkpoint）
3. **claw-ctx `assemble()` 只设置 `autoCompact: true` 标志**（`engine.ts:634`），无人消费

### 排除 v5.11.0 引入回归

- `compact()` 和 `_executeCompaction()` 方法在 v5.11.0 中**未改动**
- v5.11.0 的 LRUCache 替换仅影响缓存层，不触及触发路径
- 问题在 v5.10.0 发布前（2026-07-18 OpenClaw 升级）已潜伏，只是 v5.11.0 未修复

## 3. 修复方案

在 `afterTurn()` 末尾加自触发压缩检查。

### `src/engine.ts` 改动（`afterTurn()` 方法）

```typescript
// v5.11.1: Self-triggered compaction for ownsCompaction contract.
// Why: OpenClaw 2026.7.x skips [context-overflow-precheck] when ownsCompaction=true
// and never reads assembled.autoCompact, so claw-ctx must self-trigger in afterTurn
// to prevent token growth until timeout/overflow error.
if (p.tokenBudget && p.sessionFile && p.messages && p.messages.length > 0) {
  try {
    let estTokens = 0;
    for (const m of p.messages) {
      estTokens += this._estimateMessageTokens(m);
    }
    const triggerThreshold = Math.floor(p.tokenBudget * 0.85);
    if (estTokens > triggerThreshold) {
      this.logger.info(`[claw-ctx] afterTurn: ${estTokens} tokens > ${triggerThreshold} threshold (budget=${p.tokenBudget}), triggering self-compaction`);
      const result = await this.compact({
        sessionId: p.sessionId,
        sessionKey: p.sessionKey,
        sessionFile: p.sessionFile,
        tokenBudget: p.tokenBudget,
        currentTokenCount: estTokens,
      });
      if (result.compacted) {
        this.logger.info(`[claw-ctx] afterTurn self-compaction: ${result.result?.tokensBefore} -> ${result.result?.tokensAfter} tokens`);
      } else if (result.reason) {
        this.logger.debug?.(`[claw-ctx] afterTurn self-compaction skipped: ${result.reason}`);
      }
    }
  } catch (e: any) {
    this.logger.warn(`[claw-ctx] afterTurn self-compaction check failed: ${e?.message ?? e}`);
  }
}
```

### 设计决策

| 决策 | 理由 |
|------|------|
| 阈值 85% of `tokenBudget` | 与 `assemble()` 中已有的 `budgetLimit * 0.85` 一致 |
| 用 `_estimateMessageTokens()` 估算 | 复用已有方法，避免引入新依赖 |
| 失败只 warn 不阻断 | 与 `afterTurn()` 其他段（self-refine / checkpoint）的 best-effort 风格一致 |
| 依赖 OpenClaw 传入 `tokenBudget` | OpenClaw `selection-JInn13lc.js:7471` 已传入，契约稳定 |

## 4. 文件改动清单

| 文件 | 改动 |
|------|------|
| `src/engine.ts` | `afterTurn()` 末尾新增自触发压缩块（~30 行） |
| `tests/engine.test.ts` | 2 个新测试（触发 + 不触发） |
| `package.json` | `5.11.0` -> `5.11.1` |
| `CHANGELOG.md` | v5.11.1 条目（含根因说明） |

## 5. Test Results

```
npx tsc --noEmit  # 0 errors

npm test
Test Files  61 passed (61)
Tests       965 passed | 5 skipped (970)
```

比 v5.11.0 多 2 个新测试，0 回归。

### 新增测试

1. **`v5.11.1: afterTurn self-triggers compaction when tokens exceed 85% budget`**
   - 60 条消息 / `tokenBudget: 1000` -> 触发压缩
   - 验证 session file 行数减少
2. **`v5.11.1: afterTurn does not trigger compaction when below threshold`**
   - 短消息 / `tokenBudget: 100000` -> 不触发
   - 验证 session file 不变

## 6. Acceptance Criteria

- [x] 估算 token 超过 85% budget 时 `afterTurn()` 主动调用 `compact()`
- [x] 低于阈值时不触发（避免无谓压缩）
- [x] 压缩失败只 warn 不抛异常（best-effort）
- [x] `npm test` 通过（965/965, 0 回归）
- [x] `npx tsc --noEmit` 通过

## 7. 验证建议

**真实环境验证步骤**（需 Friday 在本地或部署环境执行）：

1. `npm run build` 生成 `dist/`
2. 重启 OpenClaw gateway（`launchctl kickstart -k gui/$(id -u)/ai.openclaw.gateway`）
3. 进行一段长对话（> 50 轮），观察 `~/Library/Logs/openclaw/*.log`：
   - 应出现 `[claw-ctx] afterTurn: N tokens > M threshold ... triggering self-compaction`
   - 紧随 `[claw-ctx] COMPACT: X msgs -> Y msgs, ... -> ≈Z tokens`
4. 检查 session file 大小是否在压缩后回落

如真实环境仍不触发，需检查 OpenClaw 是否在 `afterTurn` 调用中传 `tokenBudget`（`selection-JInn13lc.js:7471` 应已传入）。

## 8. 备注

1. **未提交 git** - 等待 Friday 决定提交策略
2. **未发布 npm** - 等待 Friday 决定版本发布
3. **OpenClaw 端无需改动** - 本修复完全在 claw-ctx 内完成
4. **向后兼容** - `afterTurn()` 签名不变，无 `tokenBudget` 时行为不变
5. **INFO.version 仍是 "5.2.0"** - 这是历史遗留（`src/engine.ts:204`），与 package.json 版本不同步。建议后续版本统一

## 9. 后续建议

1. **P2**: 同步 `INFO.version` 与 `package.json` 版本（当前 `5.2.0` vs `5.11.1`）
2. **P3**: 评估是否在 `assemble()` 返回值中移除 `autoCompact` 字段（无人消费，可能造成误导）
3. **P3**: 考虑与 OpenClaw 上游沟通，明确 `ownsCompaction: true` 时的触发责任归属（是 engine 自触发，还是 OpenClaw 主动调 `compact()`）

---

## 项目位置

`/Users/liantian/workspace/osprojects/claw-ctx`
