# Task: claw-ctx v6.4.0 — Tool Registration（独立验收 QC）

**From**: Friday (A)
**To**: Edith (C)
**Date**: 2026-08-13
**Stage**: TEST（独立验收）
**Priority**: P0
**PipelineId**: pipeline-20260805-ctxv640
**Project**: claw-ctx
**Version**: v6.4.0

---

## ⚠️ Inbox 绝对路径

```
Read:   /Users/liantian/workspace/osprojects/claw-ctx/inbox/inbox-test/
Write:  /Users/liantian/workspace/osprojects/claw-ctx/inbox/inbox-results/
Repo:   /Users/liantian/workspace/osprojects/claw-ctx/
Design: /Users/liantian/workspace/osprojects/claw-ctx/inbox/inbox-plan/v6.4.0-tool-registration-design.md
```

## 背景

本迭代把 claw-ctx 从 0 工具升级为 3 工具（ctx_compact/ctx_build/ctx_inject）。实现任务见 `inbox/inbox-code/task-v640-tool-registration.md`（Jarvis）。

## 你的验收任务（独立，不信任 Jarvis 自证）

1. **独立跑构建**：`npm run build`（含 scripts/gen-version.mjs + tsc + cp plugin.json），零错误。
2. **独立跑全量测试**：`npm test`（vitest run）全绿，核实：
   - 新增 `tests/unit/tools/*.test.ts`（≥5）+ `tests/unit/capability/inject.test.ts` 存在且被执行；
   - 基线 71 files → ≥77 files，零回归。
3. **复验工具注册**：
   - `openclaw_plugin/index.ts` 中 `api.registerTool` 被调用 3 次，name 严格为 `ctx_compact`/`ctx_build`/`ctx_inject`；
   - 三个 schema 的 type/required/enum 与设计文档一致（尤其 ctx_compact.strategy 的 enum、ctx_inject.position 的 enum）。
4. **复验清单**：`openclaw.plugin.json` 有 `contracts.tools` 含 3 工具名；version = 6.4.0。
5. **复验 inject 能力**：`ContextCapability.inject` 实现；reassign/replace 降级语义是否正确。
6. **复验版本**：`package.json` = 6.4.0；`CHANGELOG.md` 有 `[6.4.0]`。

## 验收标准

- [ ] `npm run build` 零错误
- [ ] `npm test` 全绿，新测试文件被执行
- [ ] 3 工具注册正确，name/schema 符合设计
- [ ] contracts.tools 声明正确
- [ ] ContextCapability.inject 实现正确
- [ ] 版本 6.4.0 + CHANGELOG
- [ ] 输出独立验收报告（含 build/test 摘录 + 抽查结论 + gaps）

## 输出文档

完成后在 `inbox/inbox-results/pipeline-20260805-ctxv640-test-acceptance.md` 创建验收报告。

## ⚠️ 提醒

- ✅ 独立验收，亲自跑命令核对
- ❌ 不要 git commit / push / release
- ✅ 遵循 `docs/protocol/inbox-protocol.md`

---

**启动时间**: 2026-08-13 09:29 GMT+8
