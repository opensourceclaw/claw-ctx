# Architecture Design: claw-ctx v5.16.5

**Version**: 5.16.5
**Date**: 2026-07-29
**SubStage**: PLAN > architecture-design
**Architect**: Friday (A)

---

## System Context

```
┌─────────────────────────────────────────────────────────────┐
│                      OpenClaw Gateway                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Auto-Compaction System                  │   │
│  │  ┌──────────────────────────────────────────────┐   │   │
│  │  │      ProactiveCompactionController           │   │   │
│  │  │  - checkAndCompact()                         │   │   │
│  │  │  - getModelConfig() ← NEW: getModelConfigs() │   │   │
│  │  │  - reportEnhancedError() ← NEW               │   │   │
│  │  └──────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
         ↑
         │ API calls
         │
┌────────┴────────────────────────────────────────────────────┐
│                        claw-ctx v5.16.5                      │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              ModelProfileRegistry                    │    │
│  │  - MODEL_PROFILES (35+ models)                      │    │
│  │  - getAllConfigs() ← NEW                             │    │
│  │  - getContextWindow()                                │    │
│  │  - getCompressionThreshold()                         │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │       ProactiveCompactionController                  │    │
│  │  - Enhanced error messages with drift detection      │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## Component Design

### 1. Model Config Sync API

**File**: `src/index.ts`

```typescript
// NEW: Export model configs for external sync
export interface ModelConfig {
  contextWindow: number;
  compressionThreshold: number;
  effectiveWindowRatio: number;
  proactiveThreshold: number;
}

export function getModelConfigs(): Record<string, ModelConfig>;
```

**Implementation Location**: `src/model-profile-registry.ts`

**Changes**:
- Add `getAllConfigs()` static method
- Calculate `proactiveThreshold` (75% of context window)
- Return all 35+ model configs

---

### 2. Enhanced Error Messages

**File**: `src/proactive-compaction-controller.ts`

**Error Template**:
```typescript
interface CompactionErrorContext {
  currentTokens: number;
  maxTokens: number;
  usagePercent: number;
  expectedTriggerPercent: number;
  configDrift?: {
    clawCtx: ModelConfig;
    openclaw: Partial<ModelConfig>;
  };
}

function formatEnhancedError(ctx: CompactionErrorContext): string;
```

**Error Format**:
```
⚠️ Auto-compaction failed at 113k/200k tokens (57%)

Reason: Compaction triggered too late (should trigger at 75% = 150k tokens)

Suggestions:
1. Increase reserveTokens to 50000 (25% of context)
2. Integrate ProactiveCompactionController for early detection
3. Reduce memory file loading (currently 50k tokens)
4. Sync model configs from claw-ctx (current threshold may be outdated)

Config drift detected:
- claw-ctx: GLM-5 threshold = 150k (v5.16.4)
- OpenClaw: GLM-5 threshold = not set
```

---

### 3. OpenClaw Integration Guide

**File**: `docs/openclaw-integration.md`

**Structure**:
1. Overview
2. Prerequisites
3. Integration Steps (5 steps)
4. Configuration Options
5. Verification
6. Troubleshooting

---

## API Changes

### New Exports

| Export | Type | Description |
|--------|------|-------------|
| `getModelConfigs()` | Function | Returns all model configs |
| `ModelConfig` | Interface | Model configuration shape |

### No Breaking Changes
- All existing APIs unchanged
- New APIs are additive only

---

## Data Flow

```
OpenClaw Gateway
    │
    ├─► getModelConfigs() ──► ModelProfileRegistry
    │                               │
    │                               └─► Return 35+ configs
    │
    └─► ProactiveCompactionController
            │
            ├─► checkAndCompact()
            │       │
            │       └─► Enhanced error if fails
            │
            └─► Error includes config drift detection
```

---

## Dependencies

| Package | Version | Purpose |
|---------|---------|---------|
| TypeScript | ^5.0 | Type safety |
| Node.js | >=18 | Runtime |

---

*Generated by Friday AI — 2026-07-29*
